fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
yt-dlp>=2024.12.13
faster-whisper>=1.1.0
openai>=1.51.0
pydantic>=2.7.0
aiofiles>=24.1.0
websockets>=12.0

# Only needed for mcp_server.py (agent/tool integration)
mcp>=2.0.0

# Optional — speaker diarization (heavy: pulls torch). Enable with
# ENABLE_DIARIZATION=1 + HF_TOKEN after installing:
# pyannote.audio>=3.1
